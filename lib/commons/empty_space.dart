import 'package:flutter/material.dart';

SizedBox horizontalGap({double size = 10}) => SizedBox(
      width: size,
    );

SizedBox verticallGap({double size = 10}) => SizedBox(
      height: size,
    );
